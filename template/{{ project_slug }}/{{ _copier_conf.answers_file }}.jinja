# Changes here are tracked so `copier update` can keep this project in sync.
_src_path: "{{ _src_path }}"
_commit: "{{ _commit }}"
project_name: "{{ project_name }}"
project_slug: "{{ project_slug }}"
project_description: "{{ project_description }}"
stack_choice: "{{ stack_choice }}"
ci: "{{ ci }}"
python_version: "{{ python_version }}"
with_gpu: {{ with_gpu }}
{% if with_gpu %}
pytorch_cuda_tag: "{{ pytorch_cuda_tag }}"
{% endif %}
use_external_network: {{ use_external_network }}
{% if use_external_network %}
external_network_name: "{{ external_network_name }}"
{% endif %}
{% if stack_choice == "fullstack" %}
with_node: {{ with_node }}
{% endif %}
{% if stack_choice == "agents" or (stack_choice == "fullstack" and with_node) %}
node_version: "{{ node_version }}"
nvm_version: "{{ nvm_version }}"
pnpm_version: "{{ pnpm_version }}"
{% endif %}
