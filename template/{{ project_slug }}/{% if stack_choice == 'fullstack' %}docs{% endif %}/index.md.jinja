# {{ project_name }}

{{ project_description }}

## Development

- Python version: `{{ python_version }}`
- Stack: `{{ stack_choice }}`
- CI: `{{ ci }}`

## Getting Started

1. Open the project in VS Code.
2. Reopen it in the dev container.
3. Run `uv sync`.
4. Run `uv run mkdocs serve` to preview the docs.
